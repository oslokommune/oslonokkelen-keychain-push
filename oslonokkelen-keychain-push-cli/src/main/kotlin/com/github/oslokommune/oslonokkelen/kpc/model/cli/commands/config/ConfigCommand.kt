package com.github.oslokommune.oslonokkelen.kpc.model.cli.commands.config

import com.github.ajalt.clikt.core.CliktCommand

class ConfigCommand : CliktCommand(
    help = "Cli config (this tool)",
    name = "config"
) {
    override fun run() {
    }
}
